#include <stdio.h>
/**
 * main - entry point
 *
 * prints all the numbers of base 16 in lowercase,
  *Return: 0
  */
int main(void)
{
	int c = 48;

	int j = 97;

		while(c <= 57; c++)
		{
	   putchar(c);
	  }
		while(j <= 102; j++)
		{
	    putchar(j);
	  }

		putchar('\n')
		return (0);
}
